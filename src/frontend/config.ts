/**
 * Настройки интерфейса. Всё, что касается работы с Туту и сборки маршрутов,
 * живёт в конфигах своих модулей: modules/tutu/config.ts и modules/routing/config.ts.
 *
 * Экспорты:
 * - ROUTE_EXAMPLES — примеры на пустом экране
 * - TRAVEL_CONSTRAINTS — особые условия поездки
 * - SEARCH_STAGE_MS — тайминги смены этапов поиска
 * - CITY_SUGGESTIONS_LIMIT — сколько городов в подсказках
 */

// Примеры на пустом экране: города, куда Туту не продаёт прямых билетов,
// но маршрут собирается через пересадку. Оба проверены на живых данных:
// Углич — через Ярославль, Плёс — через Иваново.
// Мышкин отсюда убран: Туту не продаёт последнее плечо Ярославль → Мышкин,
// маршрут не собирается, и показывать его на демо нельзя.
export const ROUTE_EXAMPLES = [
  { origin: 'Москва', destination: 'Углич' },
  { origin: 'Москва', destination: 'Плёс' },
];

// Особые условия поездки. Здесь только то, что реально считается по данным
// Туту. «С животным» и «нижние места» требуют схемы вагона и есть лишь у
// поездов; число детей `search_multitransport` не принимает вовсе — поэтому
// таких кнопок нет: неработающий фильтр хуже его отсутствия.
export const TRAVEL_CONSTRAINTS = [
  { key: 'noNight', label: 'Не подходит ночная поездка' },
  { key: 'noTight', label: 'Без тесных пересадок' },
];

// Виды транспорта уходят в MCP параметром `modes`: поиск сужается на стороне
// Туту, а не после. Пустой выбор означает «любой транспорт».
export const TRANSPORT_MODES = [
  { key: 'avia', label: 'Самолёт' },
  { key: 'railway', label: 'Поезд' },
  { key: 'bus', label: 'Автобус' },
  { key: 'etrain', label: 'Электричка' },
];

// Что считается ночью: выезд или прибытие в этом промежутке.
export const NIGHT_FROM_HOUR = 23;
export const NIGHT_TO_HOUR = 6;

// Тайминги смены этапов на экране поиска, миллисекунды от старта запроса.
export const SEARCH_STAGE_MS = [1200, 2300, 3400];

// Сколько городов показываем в подсказках при вводе.
export const CITY_SUGGESTIONS_LIMIT = 7;

// Через сколько после окончания поиска лента шагов сворачивается сама.
export const TRACE_COLLAPSE_DELAY_MS = 1000;

// Определение города по геопозиции: сколько ждём ответа браузера и на каком
// расстоянии город ещё считается «тем самым».
export const GEOLOCATION_TIMEOUT_MS = 8_000;
export const NEAREST_CITY_MAX_KM = 150;

// Крупный транспортный узел рядом важнее маленького города вплотную: из
// аэропорта Шереметьево ближайший город — Лобня, но уезжают оттуда из Москвы.
export const MAJOR_CITY_POPULATION = 300_000;
export const HUB_CITY_POPULATION = 1_000_000;
export const MAJOR_CITY_RADIUS_KM = 50;
